{
  "name": "homemaker-ai",
  "version": "1.0.0",
  "description": "3D Floor Plan AI Monorepo",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix mcp-server && npm run build --prefix mcp-server",
    "start": "npm start --prefix mcp-server"
  }
}
